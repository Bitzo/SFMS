link(rel='stylesheet', href='css/backend/backend.css')   

block content

  jason-pagecontent(action='/app' conf='paginationConf')
    div.page-content(stule="button {height :30px;}")
      ng-form.form-inline
        label.form-group.my-form-group
          jason-select.form-control(placeholder='应用名称'  style="width:100px;" ng-model='f.ApplicationName' source='/app')
        label.form-group.my-form-group
          button.btn.btn-sm.btn-primary(ng-click='search()') 查询
        div.buttons.pull-right
          a.btn.btn-sm.btn-success(ng-href='#/backend/applicationinfo')
            i.icon.icon-plus 新建
          button(ng-click='search()' class='btn btn-sm btn-default') 刷新
      hr
      table.table.table-striped.table-bordered.text-center
        thead
          tr
           
            th 应用ID
            th 应用代码
            th 应用名称
            th 应用备注
            th 操作
        tbody
          tr(ng-repeat="x in datas")
            
            td {{x.ID}}
            td {{x.ApplicationCode}}    
            td {{x.Memo}}
            td {{x.ApplicationName}}
            td
              a(ng-click="show(x.ID,'/app')",href='#/backend/applicationedit/' )
                i.icon.icon-edit 编辑
              a(ng-click="remove($index,'/app')")
                i.icon.icon-trash 删除

            
      div.pull-right
        jason-pagination(conf='paginationConf')

